# Paste the aos_ key from the platform console -> API Keys
AGENTOS_KEY=aos_your_key_here
# From INSIDE a container, reach the platform running on the host:
AGENTOS_URL=http://host.docker.internal:8088
